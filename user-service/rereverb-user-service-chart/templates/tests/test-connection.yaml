apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rereverb-user-service-chart.fullname" . }}-test-connection"
  labels:
    {{- include "rereverb-user-service-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rereverb-user-service-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
